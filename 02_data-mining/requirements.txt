# ============================================================
# Dependensi clustering_mining.ipynb
# Instal:  pip install -r requirements.txt
# ============================================================

# --- Inti data science ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# --- UMAP + HDBSCAN (algoritma utama) ---
umap-learn>=0.5.4
hdbscan>=0.8.33
numba>=0.57          # backend percepatan UMAP

# --- Koneksi PostgreSQL (dw_bankcredit) ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- Deep Embedded Clustering (DEC) — opsional ---
# Notebook tetap berjalan tanpa paket ini (DEC dilewati dgn estimasi konservatif).
tensorflow>=2.13     # atau: tensorflow-cpu>=2.13

# --- Opsional: pemodelan tambahan ---
# torch>=2.0         # alternatif backend DEC bila tidak memakai TensorFlow
